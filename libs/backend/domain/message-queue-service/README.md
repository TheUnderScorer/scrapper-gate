# backend-domain-message-queue-service

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test backend-domain-message-queue-service` to execute the unit tests via [Jest](https://jestjs.io).
