import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';
import {
  PrimaryIconButton,
  PrimaryLightButton,
  PrimaryLightIconButton,
} from './Buttons';
import { Button } from '@material-ui/core';
import { SaveSharp } from '@material-ui/icons';

<Meta title="UI/Buttons" component={Button} />

# Buttons

## Examples

<Canvas>
  <Story name="Primary Light Button">
    <PrimaryLightButton>Button</PrimaryLightButton>
  </Story>
</Canvas>

<Canvas>
  <Story name="Primary Icon Button">
    <PrimaryIconButton>
      <SaveSharp />
    </PrimaryIconButton>
  </Story>
</Canvas>

<Canvas>
  <Story name="Primary Light Icon Button">
    <PrimaryLightIconButton>
      <SaveSharp />
    </PrimaryLightIconButton>
  </Story>
</Canvas>
